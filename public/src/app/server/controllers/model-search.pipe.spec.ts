import { ModelSearchPipe } from './model-search.pipe';

describe('ModelSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ModelSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
