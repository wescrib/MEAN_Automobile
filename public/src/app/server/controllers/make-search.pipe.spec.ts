import { MakeSearchPipe } from './make-search.pipe';

describe('MakeSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MakeSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
